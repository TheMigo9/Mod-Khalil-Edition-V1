function onSongStart()

    setProperty('timeBar.x', 120)
    setProperty('timeBarBG.x', 120)

    setProperty('timeBar.y', 35)
    setProperty('timeBarBG.y', 35)

    setProperty('timeTxt.x', 120)
    setProperty('timeTxt.y', 20)

end